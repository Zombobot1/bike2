stages:
  - build
  - deploy

variables:
  BUILD_DIR: "build"

build:
  tags: [front]
  stage: build
  script:
    - npm i
    - npm run before-commit
  artifacts:
    paths:
      - $BUILD_DIR
    expire_in: 20 minutes

deploy:
  only:
    - master
  tags: [front]
  stage: deploy
  script:
    - npm run build
    - netlify deploy --dir=$BUILD_DIR --prod
